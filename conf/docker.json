{
  "projectKey": "${project}",
  "key": "${project}-docker",
  "rclass" : "virtual",
  "packageType": "docker",
  "repoLayoutRef" : "simple-default",
  "repositories": "${project}-docker-local,${project}-docker-remote",
  "defaultDeploymentRepo": "${project}-docker-local"
}
