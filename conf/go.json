{
  "projectKey": "${project}",
  "key": "${project}-go",
  "rclass" : "virtual",
  "packageType": "go",
  "repoLayoutRef" : "go-default",
  "repositories": "${project}-go-local,${project}-go-remote",
  "defaultDeploymentRepo": "${project}-go-local"
}
